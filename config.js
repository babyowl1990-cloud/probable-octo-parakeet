// Which site this extension serves. Change it with:  python3 set_site.py <origin> <path>
// (that also updates host_permissions in manifest.json, which must match).
const CONFIG = {
  origin: "http://localhost:8080",
  path: "/",
};
